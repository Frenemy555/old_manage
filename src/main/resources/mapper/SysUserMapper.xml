<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianya.demos.mapper.SysUserMapper">

    <resultMap id="UserResultMap" type="com.tianya.demos.pojo.entity.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT * FROM sys_user WHERE username = #{username} AND is_deleted = 0
    </select>

    <!-- 根据id查询 -->
    <select id="selectById" resultMap="UserResultMap">
        SELECT * FROM sys_user WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- 新增用户(注册) -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user(username,password,real_name,phone,role,status,is_deleted,create_time,update_time)
        VALUES(#{username},#{password},#{realName},#{phone},#{role},#{status},0,now(),now())
    </insert>

    <!-- 修改用户信息 -->
    <update id="updateById">
        UPDATE sys_user
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
            update_time = now()
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 查询所有用户 -->
    <select id="selectAll" resultMap="UserResultMap">
        SELECT id, username, real_name, phone, role, status, create_time, update_time FROM sys_user WHERE is_deleted = 0 ORDER BY create_time DESC
    </select>

    <!-- 根据id逻辑删除用户 -->
    <update id="deleteById">
        UPDATE sys_user SET is_deleted = 1 WHERE id = #{id}
    </update>

    <!-- 修改密码 -->
    <update id="updatePassword">
        UPDATE sys_user SET password = #{password}, update_time = now() WHERE id = #{userId} AND is_deleted = 0
    </update>

</mapper>
